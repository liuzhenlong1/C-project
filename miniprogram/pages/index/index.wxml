<view class="banner">
    <swiper autoplay circular indicator-dots indicator-color="rgba(255, 255, 255, .5)" indicator-active-color="#1e90ff" interval="3000">
        <block wx:for="{{bannerUrl}}" wx:key="*this">
            <swiper-item>
                <image src="{{item}}" mode="widthFix" />
            </swiper-item>
        </block>
    </swiper>
</view>
<view class="iconlist">  
    <block wx:for="{{iconList}}" wx:key="index">
        <navigator url="{{item[0]}}"><image src="{{item[1]}}" mode="widthFix" />
            <text>{{index}}</text>
        </navigator>
    </block>
</view>
<view class="item">
    <view class="title">『招聘信息』</view>
    <Tabs tabs="{{tabs}}" binditemChange="handleItemChange" />
    <block wx:for="{{item}}" wx:key="*this">
        <navigator url="null">
            <view class="item_content" bindtap="itemClick" data-index="{{index}}">
                <view class="item_jobs">{{item.jobs}}</view>
                <view class="item_company">{{item.company}}</view>
                <view class="item_treatment">
                    <view class="item_salary">{{item.salary}}</view>
                    <view class="item_treatmentlist">
                        <block wx:for="{{item.treatment.length}}" wx:for-item="x" wx:key="*this">
                            <view class="item_treatmentitem" >{{item.treatment[x]}}</view>
                        </block>
                    </view>
                </view>
            </view>
        </navigator>
    </block>
</view>